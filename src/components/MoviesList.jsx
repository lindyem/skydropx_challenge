import { useEffect, useState } from React;
import axios from axios; 

function MoviesList() {
  
   useEffect(() => {
     axios.get
   }, [])
  return (

    <div>
    </div>
  )
}

export default MoviesList;
